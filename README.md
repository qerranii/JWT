# JWT
**JWT (JSON Web Token)** — это современный «цифровой ключ» для доступа к защищённым ресурсам в интернете. Представьте, что вы приходите в гостиницу: при заселении (аутентификации) вам выдают электронную ключ-карту (токен), которая открывает только ваш номер (защищённые данные) определённое время.

Раньше сайты постоянно проверяли ваш логин и пароль при каждом действии (открытие письма, добавление товара в корзину). Это небезопасно (пароль могли перехватить) и медленно (сервер каждый раз сверял данные с базой).
JWT решает проблемы ввиде безопастности и скорости:
1. Вместо пароля вы получаете токен с ограниченным сроком жизни
2. Сервер проверяет токен самостоятельно, не обращаясь к базе данных.

Токен выглядит как длинная строка из трёх частей:
1. **Заголовок** - тип и алгоритм шифрования токена.
2. **Данные** - идентификатор пользователя, срок действия, права доступа и т.д.
3. **Подпись** - от кого был создан этот токен, гарантирует, что токен не подделан.
xxxxx.yyyyy.zzzzz
x - заголовок
y - данные
z - подпись
<img width="671" height="301" alt="image" src="https://github.com/user-attachments/assets/c34b5716-f72b-48e7-8b39-86aed27fbfc2" />

Хотя данные внутри токена можно легко прочитать, изменить их нельзя — сервер сразу обнаружит несовпадение подписи.

# Как работает на практике в веб-приложениях?
Вход в систему -> доступ к данным -> обновление токена -> выход
1. Вход в систему:
  Пользователь вводит логин и пароль → сервер проверяет их → создаёт два токена:
  Access Token (ключ) — для повседневных запросов, живёт 15-30 минут
  Refresh Token (ключ для продления) — живёт несколько дней, хранится в безопасном месте
  ```
  access_token = create_access_token(jwt_data)
  refresh_token = create_refresh_token(jwt_data)
  response.set_cookie(key="access_token", value=access_token, httponly=True)
  ```
2. Доступ к защищённым данным:
  С каждым запросом браузер автоматически отправляет access token → сервер проверяет его подпись и срок → если всё в порядке, возвращает запрошенные данные.

3. Обновление токенов:
  Когда access token «протухнет», клиент незаметно обменивает refresh token на новую пару. Пользователь продолжает работать без повторного ввода пароля.

4.  Выход из системы:
  При выходе токены удаляются, что сразу прекращает доступ:
  ```
  response.set_cookie(key="access_token", value="deleted", max_age=-1)
  ```

# Плюсы:
1. Безопасность
   Короткий срок жизни access token (15-30 минут) минимизирует риски при перехвате
   HttpOnly cookies защищают от XSS-атак
   Подпись токена гарантирует целостность данных
   Нет хранения сессий на сервере — меньше точек уязвимости

2. Производительность
   Не требует запросов к БД при каждой проверке токена
   Масштабируемость — серверы не делятся состоянием сессий
   Быстрая валидация — только криптографическая проверка подписи

3. Удобство разработки
   Самостоятельные токены содержат всю необходимую информацию  
   Простая интеграция между микросервисами 
   Кросс-доменная работа через CORS 
   Поддержка мобильных приложений

4. Пользовательский опыт
   Бесперебойная работа благодаря автоматическому обновлению токенов
   Не нужно повторно вводить пароль в течение нескольких дней
   Одновременная работа с нескольких устройств

# Минусы
1. Проблемы с отзывом токенов
  Сложно отозвать токен досрочно — требуется blacklist
  Blacklist убивает главное преимущество (отсутствие запросов к БД)
  Redis/кеш добавляет сложность архитектуры

2. Ограничения размера
  Токены могут быть большими если добавлять много данных в payload
  Увеличивает трафик — токен передаётся в каждом запросе
  Ограничение заголовков в некоторых прокси-серверах

3. Управление безопасностью
  Хранение refresh token требует особого внимания
  Риск компрометации refresh token даёт доступ на несколько дней
  Сложная настройка политик для разных клиентов (веб, мобильное приложение)

# Итог
**JWT** — это удобный и безопасный стандарт для авторизации, который:
Избавляет от постоянной передачи логина и пароля
Позволяет серверу быстро проверять права доступа
Обеспечивает автоматическое обновление сессии
Широко используется в современных веб- и мобильных приложениях
